#!/usr/bin/env bash
#MISE description="Check all packages without building"
set -e

echo "Checking morphir_models..."
cd packages/morphir_models
gleam check

echo ""
echo "Checking morphir_cli..."
cd ../morphir_cli
gleam check --target javascript
